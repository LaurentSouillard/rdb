{% extends 'layout.html.twig' %}

{% block title %}Connexion à votre espace Rue du Boulot Coworking à Paris{% endblock %}

{% block content %}
    <section id="connexion"> 
            <div class="heading">  
                    <h1>Connexion</h1>
            </div>

            <div class="row">
                    <div class="col-md-6 d-flex align-items-center mb-5">
                        <form method="post">
                            {% if error %}
                                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                            {% endif %}

                            {% if app.user %}
                                <div class="mb-3">
                                    Vous êtes connecté en tant que {{ app.user.username }}, <a href="{{ path('app_logout') }}">Déconnexion</a>
                                </div>
                            {% endif %}
                        <div class="form-group">
                            <label for="inputEmail">Email</label>
                            <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
                        </div>

                        <div class="form-group">
                            <label for="inputPassword">Mot de passe</label>
                            <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>

                            <input type="hidden" name="_csrf_token"
                                value="{{ csrf_token('authenticate') }}"
                            >
                        </div>

                        <div class="form-group">
                                    <input type="submit" class="btn" value="Soumettre">
                                    <input type="reset" class="btn ml-2" value="Réinitialiser">
                        </div>
                        <p>Pas encore membre ? <a href="{{ path('app_register') }}">Inscrivez-vous dès maintenant</a></p>
                        </form>
                    </div>
            

        {#
            Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
            See https://symfony.com/doc/current/security/remember_me.html

            <div class="checkbox mb-3">
                <label>
                    <input type="checkbox" name="_remember_me"> Remember me
                </label>
            </div>
        #}

    
        <div class="image col-md-6 d-flex align-items-center mb-5">
            <img src="images/backgroundtop.jpg" alt="Coworking à Paris" width="80%" height="65%">
        </div>
    </div>
</section>
{% endblock %}
